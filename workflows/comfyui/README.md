# ComfyUI workflow contract

These workflow JSON filenames are stable from Phase 06 onward.

- `text_to_image_v1.json`:
  Baseline text-to-image capability contract placeholder for local ComfyUI readiness checks.
- `character_refine_img2img_v1.json`:
  Reserved img2img refinement contract placeholder for later character-guided workflows.

Phase 06 validates that these files exist. Later phases replace the placeholder node graphs with real ComfyUI workflow JSON while keeping the filenames stable.
